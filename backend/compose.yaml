services:
  db:
    container_name: vovi_db
    image: 'postgres:latest'
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432
    command: postgres -c log_statement=all
